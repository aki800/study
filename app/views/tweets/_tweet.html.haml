.tweets-wrapper__box
  =image_tag "", class: "tweets-wrapper__box__avatar"
  =link_to "", class: "tweets-wrapper__box__user-name" do
    = tweet.user.nickname
  .tweets-wrapper__box__date
    = tweet.created_at
  .tweets-wrapper__box__icon
    %i{class: "fas fa-chevron-down fa-lg my-gray"}
  .tweets-wrapper__box__goal
    今日の目標  rubyの基礎を身につける
  .tweets-wrapper__box__tweet
    = tweet.text
  .tweets-wrapper__box__images
    - tweet.tweet_images.each do |tweet_images|
      = image_tag tweet_images.image.url, class: "tweets-wrapper__box__images__ele"

